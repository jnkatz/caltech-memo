name: Smoke Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  smoke-test:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Quarto
        uses: quarto-dev/quarto-actions/setup@v2

      - name: Set up TinyTeX
        uses: r-lib/actions/setup-tinytex@v2

      - name: Install Georgia and poppler (pdftotext/pdffonts assertions)
        run: |
          echo "ttf-mscorefonts-installer msttcorefonts/accepted-mscorefonts-eula select true" \
            | sudo debconf-set-selections
          sudo apt-get update
          sudo apt-get install -y ttf-mscorefonts-installer poppler-utils
          sudo fc-cache -f

      - name: Install LaTeX packages for the XeLaTeX format
        run: tlmgr install tex-gyre fontspec fancyhdr ragged2e euenc xcolor geometry

      - name: Run smoke tests (XeLaTeX + Typst)
        run: bash scripts/smoke-test.sh
